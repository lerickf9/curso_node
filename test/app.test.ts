// A A A

describe ('App', () => {
    test ('Should be 30', () => {
        expect(true).toBe(true);

        //1. Arranque
            const num1 = 20;
            const num2 = 10;
        //2. Act 
            const result = num1 + num2;
        //3. Assert
        expect(result).toBe(30);

    });
});